server:
  port: 8080

spring:
  jpa:
    show-sql: true
  datasource:
    hikari:
      connection-timeout: 2500
      max-lifetime: 600000
      maximum-pool-size: 10
      minimum-idle: 10
      maxLifetime: 300000

eureka:
  client:
    enabled: false
